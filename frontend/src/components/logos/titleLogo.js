import React from "react";
import styled from 'styled-components';

function TitleLogo(props){
    return (
        <div style={styles.container}>
            <img></img>
        </div>
    )
}

const styles = {
    container: {
        backgroundColor: '#850127',
        padding: 10,
        margin: 10,
    }
}

export default TitleLogo;
